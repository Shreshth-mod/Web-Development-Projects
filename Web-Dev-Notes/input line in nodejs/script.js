
console.log(process.argv);  // returns an array of command-line arguments passed to your Node.js script.

const a = Number(process.argv[2]);
const b =   Number(process.argv[3]);




console.log("sum " , a+b);
// This code is part of creating CLI (Command-Line Interface) tools in Node.js.